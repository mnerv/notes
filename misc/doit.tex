\documentclass[11pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}     % Fix weird character
\usepackage{geometry}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{gensymb}
\usepackage{spalign}
\usepackage{xfrac}
\usepackage{parskip}
\usepackage{float}  % figure[H]
\usepackage[style=ieee,backend=biber]{biblatex}
\usepackage[breaklinks=true,bookmarks=true,hidelinks]{hyperref}
\usepackage{tikz}
\usepackage{pgfplots}
\pgfplotsset{compat=newest,compat/show suggested version=false}

\geometry{
    a4paper,
    hmargin=2.54cm,
    tmargin=1.27cm,
    bmargin=1.27cm,
    includeheadfoot
}
\setcounter{secnumdepth}{0}  % Disable section numbering

\begin{filecontents}{doit.bib}
@misc{wiki:osi,
    author = "{Wikipedia contributors}",
    title = "OSI model --- {Wikipedia}{,} The Free Encyclopedia",
    year = "2024",
    url = "https://en.wikipedia.org/w/index.php?title=OSI_model&oldid=1219778555",
    note = "[Online; accessed 24-April-2024]"
}
\end{filecontents}
\addbibresource{doit.bib}

% https://www-net.cs.umass.edu/kurose-ross-ppt-6e/

\begin{document}
\section{Dator- och internetteknik}

\subsection{Computer Architectures}

Commonly component inside a computer architectures are inputs, outputs, memory,
arithmetic and logic unit (ALU) and a control unit.

\subsubsection{Input}

An input component handles all input into the processor. This part might be a
mouse or keyboard for human interaction or other type of input for example
communication line to another computer.

\subsubsection{Output}

The output is a way for the computer to give the result of a computation. For
example showing something on the screen.

\subsubsection{Memory}

Computer's memory is one of critical part as this might store the intruction on
how to handle input and outputs. This can also store its current state that
might be required to do more computation.

\subsubsection{Arithmetic and Logic Unit (ALU)}

This component performance calculation like the arithmetic operations and
bitwise operations on binary numbers. This part is what makes a computer
compute.

\subsubsection{Control Unit}

To coordinate everything, the control unit (CU) directs all operation on the
processor. Typical this works by using signal lines on different modules and a
decoder to decide for example if the data it is looking at is an instruction.
An instruction can be inside a memory and when read the CU have already decoder
ready to handles what modules to activate to perform the instruction.

\subsubsection{Registers}

Registers are the internal component inside a processor. They store temporary
data, usually between calculations and before moving the data around. Commonly
found registers are

\begin{itemize}
    \item Instruction Register (IR)
    \item Program Counter (PC)
    \item General Purpose Registers (GPR)
    \item Memory Address Register (MAR)
    \item Memory Data Register (MDR)
\end{itemize}


% Intterupts

% Cache Memory

% Pipelining

% CISC/RISC

% Bus

\subsubsection{Different Type of Systems}

Von-Neumann Model

General Purpose System

Parallel Processing

Flynn's Classification of Computers

\subsubsection{Instructions}

Single Instruction and Single Data Stream

Single Instruction and Multiple Data Stream

Multiple Instruction and Single Data Stream

\subsection{Computer Network}

\subsubsection{OSI Model}

The Open Systems Interconnection (OSI) model is reference model from the
International Organization for Standardization (ISO) that provide a common basis
for the coordiation of standards development for systems interconnection. OSI
model is usually describe in layers, Application layer, Presentation layer,
Session layer, Transport layer, Network layer, Data link layer, and Physical
layer. See table \ref{tb:osi} for layer name, description and example protocols.\cite{wiki:osi}

Modern networking architecture don't follow strictly the OSI model.

\begin{table}[H]
    \caption{OSI Layer}
    \centering
    \begin{tabular}{| c | c | c |}
        \hline
        Layer       & Description & Protocols                       \\
        \hline\hline
        Application &             & HTTP, SSH, Telnet, DHCP         \\
        \hline
        Presenation &             &                                 \\
        \hline
        Session     &             &                                 \\
        \hline
        Transport   &             & TCP, UDP, SCTP                  \\
        \hline
        Network     &             &                                 \\
        \hline
        Data link   &             & ARP                             \\
        \hline
        Physical    &             & RS-232, IEEE 802.3, IEEE 802.11 \\
        \hline
    \end{tabular}
    \label{tb:osi}
\end{table}

\subsection{Routing Algorithms}

Common routing algorithms in computer network are Dijkstra's algorithm,
Distance-Vector (DV) algoritm and Hierarchical routing.

\newpage
\printbibliography
\end{document}

